version: '2'
services:
  django:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
     - .:/code
    image: krother/mkr_django
    ports:
    - "8000:8000"
    command: python /code/hello_world/manage.py runserver 0.0.0.0:8000
